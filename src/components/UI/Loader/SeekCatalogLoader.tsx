import styles from "./style.module.scss";

const SeekCatalogLoader = () => {
  return (
    <div className={styles.seek__catalog_loader_container}>
      <div className={styles.seek__catalog_loader}></div>
    </div>
  );
};

export default SeekCatalogLoader;
